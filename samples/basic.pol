policy main {
    allow {
        execve
    }

    deny {
        read,
        write,
    }

    trap(10) {
        brk,
        allow
    }
}

policy other {
    terminate {
        socket
    }
}
